# Test and validation dependencies for KChat Guardrail Skills.
# Skill-pack files themselves are static YAML / JSON; these
# packages are used by the test suite, the (Phase 4) compiler,
# and the XLM-R encoder classifier adapter.
pytest>=8.0
PyYAML>=6.0
jsonschema>=4.0
# ed25519 signing / verification of compiled skill passports (Phase 4).
cryptography>=42.0
# XLM-R encoder classifier (kchat-skills/compiler/xlmr_adapter.py).
# The adapter loads an ONNX model via onnxruntime and tokenises with
# sentencepiece — no PyTorch / transformers dependency on-device. The
# adapter degrades gracefully to a SAFE fallback if any of these are
# missing.
onnxruntime>=1.16.0
sentencepiece>=0.1.99
numpy>=1.24.0
